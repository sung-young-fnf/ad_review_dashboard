apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: advertisement-dev
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: advertisement
  source:
    repoURL: https://github.com/fnf-process/ad_review_dashboard.git
    targetRevision: develop
    path: charts/advertisement
    helm:
      valueFiles:
        - values.yaml
        - values-dev.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: advertisement-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
