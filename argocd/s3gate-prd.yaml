apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: s3gate-prd
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: s3gate
  source:
    repoURL: https://github.com/fnf-process/ad_review_dashboard.git
    targetRevision: main
    path: charts/s3gate
    helm:
      valueFiles:
        - values.yaml
        - values-prd.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: s3gate-prd
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
    retry:
      limit: 3
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 5m
